/**
 * Draft Auth issuer entrypoint.
 *
 * Import the root entry when you want to create an issuer/server. Use subpath imports such as
 * `@draftlab/auth/client`, `@draftlab/auth/subject`, `@draftlab/auth/provider/*`, and
 * `@draftlab/auth/storage/*` for the other public surfaces.
 *
 * @packageDocumentation
 */
export { issuer } from "./core"
